{
    // Global settings live in vscode/ (symlinked to Code/User). This file is config's own workspace layer.
    "window.title": "⚙️ ${rootName}${separator}${activeEditorShort}${separator}${profileName}"
}

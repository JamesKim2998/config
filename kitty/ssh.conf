# Enable shell integration for clone-in-kitty (cmd+return works in SSH)
shell_integration enabled

# Apply Tokyo Night theme when connecting to Mac Mini
hostname macmini.studioboxcat.com
    color_scheme Tokyo Night

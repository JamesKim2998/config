" IdeaVim configuration for JetBrains IDEs (Rider, WebStorm, etc.)

set notimeout
nmap <Space> <Nop>  " Disable default space behavior so it can be used as leader
let mapleader = " "
set clipboard=unnamedplus

" Move lines with Option+j/k
nmap <A-j> <Action>(MoveLineDown)
nmap <A-k> <Action>(MoveLineUp)
vmap <A-j> <Action>(MoveLineDown)
vmap <A-k> <Action>(MoveLineUp)

" Jump cursor navigation
nmap <C-o> <Action>(Back)
nmap <C-i> <Action>(Forward)

" Leader mappings
nmap <leader>w <Action>(SaveAll)
nmap <leader>q <Action>(CloseContent)
nmap <leader>y <Action>(CopyPathFromRepositoryRootProvider)
nmap <leader>Y <Action>(CopyAbsolutePath)

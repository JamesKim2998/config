{
  "$schema": "../boxcat-devenv/boxcat.env.schema.json",
  "packageDirs": [
    {
      "dir": "diagnostics",
      "why": "A dotfiles repo: the root holds no package, and the TS probes that check this host's terminal, editor and latency live here."
    }
  ]
}
